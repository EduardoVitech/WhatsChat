import 'package:flutter/material.dart';

class NewMenssage extends StatelessWidget {
  const NewMenssage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Nova menssagem'),
    );
  }
}
